.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.hypothesis/
*.py[cod]
*.egg-info/
.env
.DS_Store

# Public repo: raw downloads and generated datasets stay local.
data/raw/*
!data/raw/.gitkeep
data/derived/*
!data/derived/.gitkeep
data/vintage-watchdog.log

# Generated publication artefacts are opt-in per investigation.
artifacts/

# Fetch journals are restart-safety scaffolding, not evidence: each is
# content-identical to its manifest (which IS digest-pinned in the
# governed record), and fetch order is recoverable from the manifest's
# fetched_at. Kept on disk, out of git.
investigations/*/evidence/*journal.ndjson
