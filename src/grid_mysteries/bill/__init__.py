"""The Balancing Bill (investigation 013) as a governed record.

The tracker's runner computes; this package records what it computed and
refuses to let a computed row change (`bill/bill-register.morph`). Pure
planning in `importer`, the command line in `__main__`.
"""
