"""Unattended capture of public data that publishers overwrite in place.

See ops/VINTAGE-CAPTURE.md. The package fetches, content-addresses and
writes; it never analyses, seals or proposes to Morpholog.
"""
